name: "GivEnergy Local"
version: "0.3.0"
slug: "givenergy_local"
description: "Local Modbus TCP monitoring and control for GivEnergy inverters and batteries — no cloud account needed."
url: "https://github.com/plandregan/givlocal-hassio"
arch:
  - aarch64
  - amd64
  - armv7
  - armhf
init: false
host_network: true
ingress: true
ingress_port: 8377
panel_icon: mdi:solar-power-variant
panel_title: "GivEnergy Local"
map:
  - type: data
    read_only: false
options:
  live_refresh_seconds: 30
  full_refresh_seconds: 120
schema:
  live_refresh_seconds: "int(1,60)"
  full_refresh_seconds: "int(5,300)"
